# Minimal Supabase project config for Honey Drop edge functions.
# Link with: supabase link --project-ref YOUR_REF

[functions.link-api]
# Public URL unfurler with its own SSRF checks; publishable API keys are not JWTs.
verify_jwt = false
